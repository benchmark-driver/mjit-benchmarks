prelude: |
  def a n
    x = 0
    n.times do
      n.times do
        n.times do
          n.times do
            n.times do
              n.times do
                x += 1
              end
            end
          end
        end
      end
    end
    x
  end

  x = 0

benchmark:
  nest-ntimes: x = a 18

loop_count: 18
